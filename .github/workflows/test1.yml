
  name: Test1 Workflow

  on:

    push:

      branches: master




  jobs:

      echo-test:

        name: Testing echo, env variables
        runs-on: ubuntu-latest
        steps:

          - name: Sends output

            run: |
                echo "shell used: ${SHELL}";
                echo "user name: ${USERNAME}"

            shell: bash

            env:

              USERNAME: 'Júlio César'
