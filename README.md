# Purpose of the project

To learn CI/CD with Github Actions and automate some process in both development and infrastructure sides.
To do so, I've implemented an enviroment that runs tests, build, and then deploys the project. The
workflow first prepares the runner, by installing dependencies, the project from github, and giving it
remote access to github repository.
